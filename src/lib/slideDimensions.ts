export const SLIDE_WIDTH = 960
export const SLIDE_HEIGHT = 720
export const SLIDE_ASPECT = SLIDE_WIDTH / SLIDE_HEIGHT

/** Pixels per inch on the 960×720 canvas (10in × 7.5in slide). */
export const CANVAS_PX_PER_IN = SLIDE_WIDTH / 10

/** SlideCanvas renders stored pt sizes at ×1.2 px — PDF export mirrors this. */
export const CANVAS_FONT_SCALE = 1.2
